{
    "vardefine": {
        "prefix": "vardefine",
        "body": [
            "{{#vardefine: ${1:变量名称} | ${2:变量值} }}"
        ],
        "description": "声明变量"
    },
    "vardefineecho": {
        "prefix": "vardefineecho",
        "body": [
            "{{#vardefineecho: ${:1变量名称} | ${2:变量值} }}"
        ],
        "description": "声明变量,并将这一数值打印出来"
    },
    "var": {
        "prefix": "var",
        "body": [
            "{{#var: ${1:变量名称} | ${2:默认值} }}"
        ],
        "description": "调用变量的数值,如果变量没有定义，则返回默认值，默认值可选"
    },   
    "varexists": {
        "prefix": "varexists",
        "body": [
            "{{#varexists: ${1:变量名称} }}"
        ],
        "description": "类似ifexist函数，如果变量存在则返回1（定义为空值时也会视为已存在），如果变量尚未被定义，则返回空值。"
    },
    "var_final": {
        "prefix": "var_final",
        "body": [
            "{{#var_final: ${1:变量名称} | ${2:默认值} }}"
        ],
        "description": "在Variables 2.0版本中新加入的函数功能，这个函数类似#var，只不过返回的是变量在本页面的最终值（因为一个变量可能会被多次赋值）。默认值可以作为页面变量的“兜底”，即确保了即使没有任何赋值的情况下也能使得该变量有一个默认值。"
    }  
}